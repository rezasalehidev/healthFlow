{
  "name": "@healthflow/messaging",
  "version": "0.1.0",
  "private": true,
  "description": "RabbitMQ topology, event envelopes, publisher, and retry helpers",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "jest --config jest.config.ts"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.15",
    "@nestjs/config": "^3.3.0",
    "amqplib": "^0.10.5",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.6",
    "typescript": "^5.7.2"
  },
  "peerDependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/config": "^3.0.0"
  }
}
